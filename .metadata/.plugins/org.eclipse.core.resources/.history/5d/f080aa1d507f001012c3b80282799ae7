<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>Welcome</title>
    <style>
        body {
            margin: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #fdf6e3; /* same soft cream as login page */
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            flex-direction: column;
        }

        h1 {
            color: #2c3e50; /* dark text for readability */
            margin-bottom: 40px;
            text-shadow: 1px 1px 2px rgba(0,0,0,0.1); /* subtle 3D effect */
        }

        a {
            text-decoration: none;
            padding: 15px 30px;
            background: linear-gradient(to right, #ff6b00, #ff9e00); /* same orange gradient */
            color: white;
            font-weight: bold;
            border-radius: 8px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.2); /* 3D shadow */
            transition: all 0.2s ease;
        }

        a:hover {
            transform: translateY(-3px); /* lift effect */
            box-shadow: 0 8px 20px rgba(0,0,0,0.3); /* stronger 3D shadow on hover */
        }
    </style>
</head>
<body>
    <h1>Welcome to SecureNetBank</h1>
    <a th:href="@{/login}">Login</a>
</body>
</html>
